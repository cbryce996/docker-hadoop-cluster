<configuration>

    <!-- DataNode specific configurations -->

    <!-- Directory where the DataNode stores its blocks -->
    <property>
        <name>dfs.data.dir</name>
        <value>/data/datanode</value>
    </property>

    <property>
        <name>dfs.name.dir</name>
        <value>/data/namenode</value>
    </property>

    <!-- Address on which the DataNode will listen for connections from other nodes -->
    <property>
        <name>dfs.datanode.address</name>
        <value>datanode:10019</value>
    </property>
        
    <property>
        <name>dfs.datanode.http.address</name>
        <value>datanode:10022</value>
    </property>

    <property>
        <name>dfs.datanode.https.address</name>
        <value>datanode:10025</value>
    </property>

    <property>
        <name>dfs.data.transfer.protection</name>
        <value>integrity</value>
    </property>

    <property>
        <name>dfs.block.access.token.enable</name>
        <value>true</value>
    </property>

    <property>
        <name>dfs.webhdfs.enabled</name>
        <value>true</value>
    </property>

    <property>
        <name>dfs.https.enable</name>
        <value>true</value>
    </property>

    <property>
        <name>dfs.http.policy</name>
        <value>HTTPS_ONLY</value>
    </property>

    <!-- Kerberos authentication settings -->

    <property>
        <name>dfs.datanode.keytab.file</name>
        <value>/keys/hdfs-datanode.keytab</value>
    </property>

    <property>
        <name>dfs.namenode.keytab.file</name>
        <value>/keys/hdfs-namenode.keytab</value>
    </property>

    <property>
        <name>dfs.datanode.kerberos.principal</name>
        <value>hdfs/datanode@CLUSTER</value>
    </property>

    <property>
        <name>dfs.namenode.kerberos.principal</name>
        <value>hdfs/namenode@CLUSTER</value>
    </property>

</configuration>
